<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools">

    <View
        android:id="@+id/topHeaderWaitlist"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/yellow"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.1"/>

    <TextView
        android:id="@+id/textWaitlistHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Event Waitlist"
        android:textSize="28sp"
        android:textColor="@android:color/white"
        android:gravity="center"
        android:fontFamily="@font/roboto_slab"
        app:layout_constraintTop_toTopOf="@id/topHeaderWaitlist"
        app:layout_constraintBottom_toBottomOf="@id/topHeaderWaitlist"/>
    <ImageButton
        android:id="@+id/buttonBack"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_arrow_back"
        android:contentDescription="Back"
        app:layout_constraintTop_toTopOf="@id/topHeaderWaitlist"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/topHeaderWaitlist"/>
    <HorizontalScrollView
        android:background="@color/beige"
        android:id="@+id/statusChipScroll"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:fillViewport="true"
        android:scrollbars="none"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/topHeaderWaitlist"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/statusChipGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:chipSpacing="8dp"
            app:singleSelection="false">

            <com.google.android.material.chip.Chip
                android:id="@+id/chipWaiting"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Waiting"
                android:checked="true"
                android:checkable="true"
                app:chipBackgroundColor="@drawable/chip_background_selector"
                android:textColor="@drawable/chip_text_selector"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipSelected"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Selected"
                android:checkable="true"
                app:chipBackgroundColor="@drawable/chip_background_selector"
                android:textColor="@drawable/chip_text_selector"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipAccepted"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Accepted"
                android:checkable="true"
                app:chipBackgroundColor="@drawable/chip_background_selector"
                android:textColor="@drawable/chip_text_selector"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipDeclined"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Declined"
                android:checkable="true"
                app:chipBackgroundColor="@drawable/chip_background_selector"
                android:textColor="@drawable/chip_text_selector"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipCancelled"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Cancelled"
                android:checkable="true"
                app:chipBackgroundColor="@drawable/chip_background_selector"
                android:textColor="@drawable/chip_text_selector"/>
        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

    <LinearLayout
        android:background="@color/beige"
        android:id="@+id/actionButtonsLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/statusChipScroll"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonShowGeo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Show Geolocations"
            app:backgroundTint="@color/green"
            android:textColor="@android:color/white"
            app:cornerRadius="8dp"
            android:layout_marginEnd="16dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonSendNotifications"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Send Notifications"
            app:cornerRadius="8dp"
            app:backgroundTint="@color/green"
            android:textColor="@android:color/white"/>
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:background="@color/beige"
        android:id="@+id/recyclerWaitlist"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/actionButtonsLayout"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/item_waitlist_card"/>
</androidx.constraintlayout.widget.ConstraintLayout>
